import Vue from 'vue';
import d3 from 'd3';
import c3 from 'c3';
import App from './App.vue';

// Create our Vue App and replace the <div id="app"></div> with it
new Vue({
  el: '#app',
  render: h => h(App)
});
